# AGENTS.md

## Cursor Cloud specific instructions

This repository (`aws-admin-cursor`) is currently empty — it contains only a `README.md`. No application code, dependencies, or services exist yet.

- **No build/lint/test commands** are available. Once application code is added, update this section with the relevant commands.
- **No services** need to be started for development at this time.
- **Update script**: The VM update script is a no-op (`echo "No dependencies to install"`). Replace it once a dependency manifest (e.g. `package.json`, `requirements.txt`) is added.
